# المساهمة في مشروع Web3 Jobs 🤝

شكراً لاهتمامك بالمساهمة في مشروع Web3 Jobs! نقدر كل مساعدة ونتطلع للعمل معك.

## كيفية المساهمة

### الإبلاغ عن المشاكل 🐛

إذا وجدت مشكلة في المشروع، يرجى:
1. التحقق من عدم وجود issue مماثل بالفعل
2. إنشاء issue جديد وصف المشكلة بوضوح
3. تضمين:
   - وصف تفصيلي للمشكلة
   - خطوات إعادة إنتاج المشكلة
   - لقطات شاشة إن أمكن
   - معلومات النظام (المتصفح، نظام التشغيل)

### طلب ميزات جديدة ✨

هل لديك فكرة لميزة جديدة؟ رائع! يرجى:
1. التحقق من عدم وجود request مماثل
2. إنشاء issue جديد وصف الميزة المقترحة
3. شرح سبب أهمية هذه الميزة
4. تقديم أفكار حول كيفية تنفيذها إن أمكن

### المساهمة بالكود 💻

#### خطوات البدء:

1. **Fork المشروع**
   ```bash
   # اضغط على زر Fork في صفحة المشروع على GitHub
   ```

2. **استنساخ المشروع**
   ```bash
   git clone https://github.com/YOUR-USERNAME/Web3-Jobs.git
   cd Web3-Jobs
   ```

3. **إنشاء فرع جديد**
   ```bash
   git checkout -b feature/your-feature-name
   ```

4. **تثبيت الاعتماديات**
   ```bash
   npm install
   ```

5. **تشغيل المشروع**
   ```bash
   npm run dev
   ```

#### معايير الكود:

- اتبع معايير الكود الموجودة في المشروع
- استخدم TypeScript للأمان من النوع
- اتبع أفضل ممارسات React
- أضف تعليقات للكود المعقد
- تأكد من أن الكود يعمل بشكل صحيح

#### تقديم Pull Request:

1. **تأكد من تحديث فرعك**
   ```bash
   git fetch upstream
   git rebase upstream/main
   ```

2. **التزم بتغييراتك**
   ```bash
   git add .
   git commit -m "feat: add new feature"
   ```

3. **ادفع التغييرات**
   ```bash
   git push origin feature/your-feature-name
   ```

4. **إنشاء Pull Request**
   - اذهب إلى صفحة المشروع على GitHub
   - اضغط على "New Pull Request"
   - املأ القالب المطلوب
   - انتظر المراجعة

## معايير قبول المساهمات

- الكود يجب أن يكون مكتوباً بلغة TypeScript
- يجب أن يتبع معايير ESLint الموجودة
- يجب أن يكون الكود موثقاً بشكل جيد
- يجب أن لا يكسر أي وظائف موجودة
- يجب أن يكون الوصف في Pull Request واضحاً ومفصلاً

## التواصل

إذا كانت لديك أي أسئلة، لا تتردد في:
- فتح issue للمناقشة
- التواصل معنا عبر Pull Request
- مراجعة التوثيق

## شكراً لك! 🎉

نقدر وقتك وجهدك في المساهمة في مشروع Web3 Jobs!
