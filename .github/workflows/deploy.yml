name: Deploy to GitHub Pages (disabled - use static.yml)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: This workflow is disabled
        run: |
          echo "This deploy workflow has been disabled to avoid conflicts."
          echo "Use .github/workflows/static.yml for GitHub Pages deployment."
